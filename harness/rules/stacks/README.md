# Stack Adaptation Rules

Create language, framework, database, cloud, or client-specific rules here. Each rule declares scope paths, project-profile dependencies, actual commands, tool versions, exception approver, and baseline mapping.

Stack rules may strengthen constraints or map abstract rules to tools; they must not weaken the baseline policy, contract, or registry.
