# Debug Workflow

Define symptom, expectation, impact, and observable evidence. Reproduce in isolation; record environment and eliminated causes if reproduction fails. Trace input/interface, domain logic, data, external dependencies, and presentation. Fix the smallest correct owner; write an ADR for architectural issues. Re-run reproduction, related tests, and gates. Convert repeated issues into tests, rules, hooks, checks, or Skills.
