# Setup Workflow

Read the project profile; confirm declared entrypoint, dependency source, test data, and environment boundary. Check tools and prerequisites, but do not self-install runtime dependencies. Obtain approval before services, test resources, migrations, or network access. Run declared commands, verify health and a minimal local test, and record commands, environment, result, and unresolved prerequisites.
