# 质量门禁例外策略

仅 `REQUIRED` 与 `CONDITIONAL` 规则允许例外；`BLOCKER` 规则不可豁免。

## 例外记录必填项

- 规则 ID 与适用范围
- 业务原因和不可替代性
- 风险等级及受影响系统/数据
- 补偿控制和验证证据
- 审批人、创建日期、到期日、复查日期
- 移除例外的明确计划

例外最长有效期为 30 天。例外过期、审批缺失、风险描述不完整或补偿控制无法验证时，门禁自动恢复阻断。例外不得用于规避法律、平台条款、认证授权、安全红线或生产访问审批。
