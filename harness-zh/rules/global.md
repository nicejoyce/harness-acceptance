# 全局规则

- 所有完成结论以实际输出、报告或制品摘要为证据，遵循 `DOC-001` 与 `DOC-006`。
- 外部输入、秘密、错误处理、资源释放和权限检查遵循 `SEC-*`、`DATA-*`、`BOUND-*`。
- 默认组合优先、依赖单向、领域逻辑可测试，遵循 `ARCH-*`。
- 项目文件、语言、框架和包管理器的具体要求由 `rules/stacks/` 与项目画像补充；不得弱化基线。
- 任何不可恢复、生产、网络副作用或敏感数据操作均先依据安全策略取得审批。
