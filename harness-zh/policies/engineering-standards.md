# 工程标准

- 仅通过已批准的包管理器固定和更新依赖版本；不手工编辑锁文件。
- 每个外部边界都要定义输入校验、超时、取消、错误传播和资源释放。
- 公共契约必须版本化并保持兼容，或明确迁移和弃用方案。
- 使用最小权限、参数化数据访问、加密的秘密存储和不含敏感信息的结构化日志。
- 单元测试必须可重复；集成和端到端测试必须使用显式环境与受控测试数据。
- 优先保证可复现构建；发布记录工具版本、制品摘要和验证证据。
- 重大变更需要人工同行评审；高风险变更需要安全审查。
